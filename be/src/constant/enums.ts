export enum STATUS {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE'
}

export enum EVENT_SOCKET {
  UNIT = 'UNIT',
  RESOURCE = 'RESOURCE',
  BUILDING = 'BUILDING',
  TRAINING = 'TRAINING',
}

export enum BUILDING {
  STORAGE = "STORAGE",
}