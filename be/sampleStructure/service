import { AbstractService } from "abstracts"
import { InterfaceName, IUser, IWorld } from "interfaces"
export default class ServiceName extends AbstractService<InterfaceName>  {
  constructor(user: IUser & { world: IWorld }) {
    super(ModelName, user)
  }
}